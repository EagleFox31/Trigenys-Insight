name: Cloudflare compatibility

on:
  pull_request:
    paths:
      - 'src/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'next.config.ts'
      - '.github/workflows/cloudflare-compatibility.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  vinext-check:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run vinext compatibility check
        run: pnpm cloudflare:check

      - name: Build vinext production output
        env:
          NEXT_PUBLIC_SERVER_URL: https://trigenys-insight-cloudflare-pilot.example.workers.dev
          DATABASE_URL: postgresql://placeholder:placeholder@localhost:5432/placeholder
          DATABASE_URL_UNPOOLED: postgresql://placeholder:placeholder@localhost:5432/placeholder
          PAYLOAD_SECRET: cloudflare-pilot-build-only-secret
          CRON_SECRET: cloudflare-pilot-build-only-cron-secret
          PREVIEW_SECRET: cloudflare-pilot-build-only-preview-secret
        run: pnpm build:vinext

      - name: Generate Cloudflare config ephemerally
        run: pnpm dlx vinext init --platform=cloudflare --skip-check

      - name: Show vinext init changes
        run: |
          echo "===== package.json ====="
          git diff -- package.json
          echo "===== generated files ====="
          for file in vite.config.ts wrangler.jsonc; do
            if [ -f "$file" ]; then
              echo "----- $file -----"
              cat "$file"
            fi
          done
          echo "===== extra generated files ====="
          git status --short
          for file in empty-stub.js; do
            if [ -f "$file" ]; then
              echo "----- $file -----"
              cat "$file"
            fi
          done
          echo "===== gitignore diff ====="
          git diff -- .gitignore
